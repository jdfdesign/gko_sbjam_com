<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <%= render_page_title(resource.title) %> 
      <%= render_slideshow_for(resource) %>
      <%= render_copy_text(resource.body) %>
    </div>
  </div> 
</div>